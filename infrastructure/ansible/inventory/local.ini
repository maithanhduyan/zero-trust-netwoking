# Local test inventory
# For testing on localhost before deploying to real servers

[hub]
localhost ansible_connection=local ansible_host=127.0.0.1

[app]
# No app servers in local test

[db]
# No db servers in local test

[ops]
# No ops servers in local test

[agent:children]
app
db
ops

[zt_network:children]
hub
agent

[all:vars]
ansible_python_interpreter=/usr/bin/python3
