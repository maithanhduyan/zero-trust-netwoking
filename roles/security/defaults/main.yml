---
# =============================================================================
# SECURITY ROLE - Default Variables
# =============================================================================

ssh_port: 22

ufw_default_incoming: "deny"
ufw_default_outgoing: "allow"
ufw_logging: "on"

ufw_allowed_ports:
  - { port: 22, proto: "tcp", from: "100.64.0.0/10" }
  - { port: 41641, proto: "udp", from: "any" }
